type Channel @entity {
  id: ID!
  walletA: Bytes!
  walletB: Bytes!
  proxyWalletA: Bytes!
  proxyWalletB: Bytes!
  amountA: BigInt!
  amountB: BigInt!
  metadata: Bytes
  status: ChannelStatus!
}

enum ChannelStatus {
  CREATED
  ACTIVE
  PENDING
  CLOSED
}
