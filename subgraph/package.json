{
  "name": "payment_channel",
  "license": "UNLICENSED",
  "scripts": {
    "prepare": "graph codegen && graph build --network",
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service",
    "deploy": "graph deploy --product hosted-service erroders/payment_channel"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.63.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
